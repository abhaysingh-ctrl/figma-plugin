# Shared secret the plugin sends as the `x-sitemap-secret` header. Generate a
# long random string (e.g. `openssl rand -hex 32`) — this is the only abuse
# control on the public endpoint.
SITEMAP_SHARED_SECRET=change-me-to-a-random-string
